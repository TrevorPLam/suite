name: Chromatic

on:
  pull_request:
    branches: [main]
  push:
    branches: [main]

jobs:
  chromatic:
    name: Run Chromatic
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 9

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Run Chromatic
        uses: chromaui/action@latest
        with:
          # Chromatic project token (must be configured in GitHub secrets)
          projectToken: ${{ secrets.CHROMATIC_PROJECT_TOKEN }}
          # Working directory for the UI package (monorepo setup)
          workingDir: packages/ui
          # Script to build Storybook
          buildScriptName: build-storybook
          # Exit with zero even if changes are found (Chromatic will report status check)
          exitZeroOnChanges: true
