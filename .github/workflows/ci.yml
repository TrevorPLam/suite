name: CI

on:
  pull_request:
    branches: [main]
  push:
    branches: [main]

jobs:
  # PR checks: Fast validation using affected tests only
  pr-check:
    if: github.event_name == 'pull_request'
    runs-on: ubuntu-latest
    strategy:
      matrix:
        node-version: [20.x]
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 9

      - name: Setup Node.js ${{ matrix.node-version }}
        uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node-version }}
          cache: 'pnpm'

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Run affected unit tests
        run: nx affected -t test --base=main~1

      - name: Run affected typecheck
        run: nx affected -t typecheck --base=main~1

      - name: Run affected lint
        run: nx affected -t lint --base=main~1

      - name: Verify API dry-runs
        run: |
          cd apps/calendar/api && npx wrangler deploy --dry-run
          cd ../drive/api && npx wrangler deploy --dry-run
          cd ../tasks/api && npx wrangler deploy --dry-run

      - name: Verify web builds
        run: |
          cd apps/calendar/web && npx vite build
          cd ../drive/web && npx vite build
          cd ../tasks/web && npx vite build

  # Main branch validation: Full test suite with coverage
  main-validation:
    if: github.event_name == 'push'
    runs-on: ubuntu-latest
    services:
      postgres:
        image: postgres:14
        env:
          POSTGRES_PASSWORD: postgres
          POSTGRES_DB: suite
        ports:
          - 5432:5432
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
    strategy:
      matrix:
        node-version: [20.x]
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 9

      - name: Setup Node.js ${{ matrix.node-version }}
        uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node-version }}
          cache: 'pnpm'

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Lint database migrations
        run: pnpm --filter @suite/db lint:migrations

      - name: Run database migrations
        run: pnpm db:migrate
        env:
          APP_DOMAIN: localhost
          DATABASE_URL: postgresql://postgres:postgres@localhost:5432/suite

      - name: Run full test suite
        run: pnpm test:run
        env:
          DATABASE_URL: postgresql://postgres:postgres@localhost:5432/suite

      - name: Run typecheck
        run: pnpm typecheck
        env:
          DATABASE_URL: postgresql://postgres:postgres@localhost:5432/suite

      - name: Run lint
        run: pnpm lint

      - name: Verify API dry-runs
        run: |
          cd apps/calendar/api && npx wrangler deploy --dry-run
          cd ../drive/api && npx wrangler deploy --dry-run
          cd ../tasks/api && npx wrangler deploy --dry-run

      - name: Verify web builds
        run: |
          cd apps/calendar/web && npx vite build
          cd ../drive/web && npx vite build
          cd ../tasks/web && npx vite build

      - name: Run coverage
        run: pnpm test:coverage

      - name: Upload coverage reports
        uses: codecov/codecov-action@v4
        with:
          files: ./coverage/lcov.info
          flags: unittests
          name: codecov-umbrella

  # E2E tests: Run Playwright tests
  e2e:
    if: github.event_name == 'push'
    runs-on: ubuntu-latest
    strategy:
      matrix:
        node-version: [20.x]
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 9

      - name: Setup Node.js ${{ matrix.node-version }}
        uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node-version }}
          cache: 'pnpm'

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Install Playwright browsers
        run: pnpm exec playwright install --with-deps chromium firefox webkit

      - name: Run E2E tests
        run: pnpm exec playwright test
        env:
          DATABASE_URL: postgresql://postgres:postgres@localhost:5432/suite

      - name: Upload Playwright report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: playwright-report
          path: playwright-report/
          retention-days: 30

      - name: Upload Playwright screenshots
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: playwright-screenshots
          path: test-results/
          retention-days: 30
